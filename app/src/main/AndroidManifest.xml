<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hammer.learandroid">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/app_name">
        </activity>

        <activity android:name=".activity.MainActivity"
            >

        </activity>

        <activity android:name="com.fungame.hammer.lib.UIActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.LessonTwoActivity"/>

        <activity android:name=".activity.LessonThreeActivity"
            android:theme="@style/AppTheme.WithoutActionBar"
            />
        <activity android:name=".activity.LessonFourActivity"
            android:theme="@style/AppTheme.WithoutActionBar"
            />
        <activity android:name=".activity.LessonFiveAvtivity"
            android:theme="@style/AppTheme.WithoutActionBar"
            />
        <activity android:name=".activity.LessonSixActivity"/>
        <activity android:name=".activity.Lesson7Activity"
            android:theme="@style/AppTheme.WithoutActionBar"/>
    </application>

</manifest>
